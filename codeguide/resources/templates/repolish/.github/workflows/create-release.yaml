# generated by hotdog-werx/codeguide - manual changes will be overwritten
name: create-release

on:
  pull_request:
    types: [closed]
    branches: [master]

jobs:
  github-release:
    uses: hotdog-werx/repolish/.github/workflows/_create-release.yaml@{{ cookiecutter.codeguide_ref }}
    secrets: inherit
    with:
      build-command: '{{ cookiecutter.build_command }}'
      publish-command: '{{ cookiecutter.publish_command }}'

## repolish-regex[additional-jobs]: ^## post-release jobs([\s\S]*)$
## post-release jobs - add your custom jobs here