# generated by hotdog-werx/codeguide - manual changes will be overwritten
name: ci-checks

on:
  pull_request:
  push:
    branches: [master]

jobs:
  run-checks:
    uses: hotdog-werx/repolish/.github/workflows/_run-checks.yml@{{ cookiecutter.codeguide_ref }}
    with:
      operating-systems: '{{ cookiecutter.ci_operating_systems }}'

## repolish-regex[additional-jobs]: ^## post-check jobs([\s\S]*)$
## post-check jobs - add your custom jobs here
